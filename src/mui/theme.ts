import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#113047',
    },
    secondary: {
      main: '#fff',
    },
    warning: {
      main: '#eadd15',
    },
    error: {
      main: '#ff4242',
    },
    success: {
      main: '#3ab05b',
    },
    info: {
      main: '#308dcf',
    },
  },
});

export default theme;
